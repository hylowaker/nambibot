# 명령

- `/music help`
- `/music join`
- `/music queue`
- `/music show`
- `/music delete`
- `/music purge`
- `/music play`
- `/music stop`
- `/music skip`
- `/music leave`
- `/version`


## `/music help`

사용 가능한 명령어 목록과 간략한 도움말을 출력한다.


## `/music join <CHANNEL>`

_예시:_ `/music join 음성채팅방`

파라미터:
- CHANNEL: (Optional) Discord 음성 채널 이름.

음성 채널에 봇이 참가한다. CHANNEL 파라미터로 봇이 참가할 채널 이름을 지정할 수 있다.

CHANNEL 파라미터 없이 실행시, 명령을 실행한 사용자와 동일한 음성 채널에 참가한다.

사용자가 음성 채널에 참가 중이 아닌 경우, 첫번째 음성 채널(서버 채널 목록 중 가장 위에 있는 채널)에 참가한다.


## `/music queue <URL>`

_예시:_ `/music queue https://youtu.be/dQw4w9WgXcQ`

파라미터:
- URL: (Required) 음악/영상 URL.

대기열의 마지막에 항목을 추가한다.

현재 봇이 음성 채널에 참가 중이 아닌 경우, 자동으로 음성 채널에 참가한다. (참가 기준은 `/music join`과 동일.)

현재 대기열이 비어있고, 봇이 음성 채널에 참가 중이고, 아무 음악도 재생 중이지 않은 경우, 추가한 항목을 즉시 재생한다.
이 조건에 해당하지 않는 경우에는 대기열에 추가만 한다.

URL이 올바른 음악 URL이 아닌 경우, 에러 메시지를 출력하고 아무 작업도 하지 않는다.

URL이 여러 항목을 포함하는 경우 (예: YouTube 재생 목록) 전체 항목을 대기열에 추가한다.


## `/music show`

현재 재생 중인 항목과 대기열 내용을 출력한다.


## `/music delete <INDEX>`

_예시:_ `/music delete 3`

파라미터:
- INDEX: (Optional) 대기열의 항목 인덱스.

대기열에서 INDEX 번째에 있는 항목을 삭제한다.

대기열이 비어있거나, INDEX가 범위를 벗어나는 경우, 에러 메시지를 출력하고 아무 작업도 하지 않는다.

INDEX 파라미터 없이 실행시, 대기열에서 가장 마지막 항목을 삭제한다.

이 명령은 봇이 음성 채널에 참가 중일 때에만 사용할 수 있다.
현재 음성 채널에 참가 중이 아닌 경우, 에러 메시지를 출력하고 아무 작업도 하지 않는다.


## `/music purge`

현재 대기열을 전부 삭제한다.

(현재 재생 중인 음악에는 영향을 미치지 않는다.)

이 명령은 봇이 음성 채널에 참가 중일 때에만 사용할 수 있다.
현재 음성 채널에 참가 중이 아닌 경우, 에러 메시지를 출력하고 아무 작업도 하지 않는다.


## `/music play <INDEX>`

_예시:_ `/music play 3`

파라미터:
- INDEX: (Optional) 대기열의 항목 인덱스.

대기열에서 INDEX 번째에 있는 항목을 제거하고 해당 음악을 즉시 재생한다.

INDEX 파라미터 없이 실행시, 대기열의 첫번째 항목이 선택된다.

대기열이 비어있거나, INDEX가 범위를 벗어나는 경우, 에러 메시지를 출력하고 아무 작업도 하지 않는다.

현재 이미 재생 중인 음악이 있는 경우에는 그 음악이 중단된다.

이 명령은 봇이 음성 채널에 참가 중일 때에만 사용할 수 있다.
현재 음성 채널에 참가 중이 아닌 경우, 에러 메시지를 출력하고 아무 작업도 하지 않는다.


## `/music stop`

현재 음악을 재생 중인 경우, 음악 재생을 중단한다.

현재 음악 재생 중이 아닌 경우에는 아무 작업도 하지 않는다.

이 명령은 봇이 음성 채널에 참가 중일 때에만 사용할 수 있다.
현재 음성 채널에 참가 중이 아닌 경우, 에러 메시지를 출력하고 아무 작업도 하지 않는다.


## `/music skip`

`/music stop`과 `/music play`를 연속으로 실행한 것과 같다.

현재 재생 중인 음악을 즉시 중단하고 대기열의 첫번째 항목을 재생한다.

이 명령은 봇이 음성 채널에 참가 중일 때에만 사용할 수 있다.
현재 음성 채널에 참가 중이 아닌 경우, 에러 메시지를 출력하고 아무 작업도 하지 않는다.


## `/music leave`

음성 채널에서 봇이 퇴장한다.

이 명령은 봇이 음성 채널에 참가 중일 때에만 사용할 수 있다.
현재 음성 채널에 참가 중이 아닌 경우, 에러 메시지를 출력하고 아무 작업도 하지 않는다.


## `/version`

_package.json_ 파일을 읽고 현재 봇의 버전과 설명을 출력한다.

현재 JavaScript 런타임과 의존성(yt-dlp, ffmpeg) 정보도 출력한다.
