DISCORD_TOKEN=  # 봇 토큰
APPLICATION_ID=  # 애플리케이션(클라이언트) ID
GUILD_ID=  # 서버(길드) ID

DEVELOPE_PREFIX=  # 개발 모드 (ON 또는 1이면 /dev-music, /dev-version으로 등록)

WEB_PORT=3000  # Web UI 포트 (기본값: 3000)
WEB_PASSWORD=  # Web UI 접근 비밀번호 (비워두면 인증 없이 접근 가능)

NAMBI_DIR=     # 데이터 저장 경로 (대기열, 로그 등, 기본값: ~/.nambi)
